<UserControl
    x:Class="Clickra_Fluent.VisualSplitterControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="240"
    d:DesignWidth="540"
    Width="540"
    Height="240">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Mode bar -->
        <Grid Grid.Row="0" ColumnSpacing="8" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="ModeCombo" Grid.Column="0" HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <TextBlock x:Name="NLabel" Text="每檔頁數" VerticalAlignment="Center"/>
                <NumberBox x:Name="NBox" Width="80" SpinButtonPlacementMode="Inline" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Body: segment list + preview -->
        <Grid Grid.Row="1" ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="185"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: segment list -->
            <Grid Grid.Column="0" RowSpacing="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock x:Name="SegmentHeader" Text="分段列表" FontWeight="SemiBold" Margin="4,0,0,0"/>
                <ListView x:Name="SegmentList" Grid.Row="1" SelectionMode="Single" BorderThickness="1"/>
                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="4" Margin="0,3,0,0">
                    <Button x:Name="AddSegmentBtn" Content="＋ 新增" Padding="8,2"/>
                    <Button x:Name="DeleteSegmentBtn" Content="刪除" Padding="8,2"/>
                    <Button x:Name="ClearSegmentsBtn" Content="清空" Padding="8,2"/>
                </StackPanel>
            </Grid>

            <!-- Right: preview -->
            <Grid Grid.Column="1" RowSpacing="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Spacing="4" HorizontalAlignment="Center">
                    <Button x:Name="PrevPageBtn" Content="&#x25C0;" MinWidth="30" Padding="6,2"/>
                    <TextBlock x:Name="PageLabel" VerticalAlignment="Center" MinWidth="72" TextAlignment="Center"/>
                    <Button x:Name="NextPageBtn" Content="&#x25B6;" MinWidth="30" Padding="6,2"/>
                    <Button x:Name="SplitAtPageBtn" Content="切開" Padding="8,2"/>
                </StackPanel>
                <Border Grid.Row="1" BorderThickness="1" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Grid>
                        <ScrollViewer x:Name="ZoomHost" Visibility="Collapsed" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollMode="Enabled" VerticalScrollMode="Enabled" ZoomMode="Disabled">
                            <Image x:Name="ZoomImage" Stretch="None"/>
                        </ScrollViewer>
                        <Image x:Name="FitImage" Stretch="Uniform"/>
                    </Grid>
                </Border>
                <ToggleButton x:Name="ZoomToggle" Grid.Row="2" Content="放大檢視" HorizontalAlignment="Center"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
